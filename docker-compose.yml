version: "3.8"

services:
  app:
    build: .
    container_name: graphql-app
    ports:
      - "8080:8080"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017

  mongo:
    image: mongo:7
    container_name: mongodb
    ports:
      - "27017:27017"